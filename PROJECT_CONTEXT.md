# 나잘알 프로젝트 컨텍스트

> 기준 대화: ChatGPT `소림파워 전용 앱 상담`에서 앱 개발 관련 내용만 정리
> 정리일: 2026-08-18
> 목적: 새 Codex 작업이 기존 철학, 현재 구현, UX 개편 요구사항과 우선순위를 빠르게 파악하도록 하는 인수인계 문서

## 1. 제품 정체성

`나잘알`은 사용자의 생활·행동·관심 데이터를 모아 **나를 객관적으로 관찰하고, 시간에 따른 변화를 발견하며, 하나의 정답 대신 여러 관점과 선택지를 보여주는 개인용 Android 앱**이다.

핵심 철학은 다음과 같다.

- 사용자를 성격·정치 성향·편향 점수 같은 하나의 유형으로 고정하지 않는다.
- AI는 판단자가 아니라 `관찰 → 구조화 → 비교 → 놓친 관점 발견 → 다른 길 제안` 역할을 한다.
- 현재 상태의 점수보다 시간에 따른 변화와 궤적을 보여준다.
- 사용자가 무엇을 봤는지 복제해 나열하기보다, 그 콘텐츠가 어떤 관심과 관점을 만들었는지 보여준다.
- 기능 설명과 AI 모델명을 전면에 내세우지 않고 실제 사용자 데이터와 발견을 주인공으로 삼는다.
- 건강·투자·부동산·콘텐츠 등 민감한 영역에서도 단정하거나 훈계하지 않는다.

유튜브 분석 기능의 한 문장 정의:

> 내가 요즘 무엇을 보고 있는지 뇌 지도에서 한눈에 보고, 관심 주제를 누르면 어떤 관점으로 봤는지 탐색하며, 필요할 때 더 구체적인 질문과 새로운 주제를 추천받는다.

## 2. 이름과 브랜딩

- 기존 이름은 `일당백`이었으나, 기능이 많은 만능 앱 인상보다 자기 관찰·변화·성장을 드러내기 위해 `나잘알`로 전환했다.
- `나잘알`의 의미는 “앱이 나를 단정한다”가 아니라 **앱을 쓰며 사용자가 점점 자기 자신을 잘 알게 된다**는 것이다.
- 활용 가능한 문구: `오늘도 나잘알 +1`, `이번 달 나잘알 리포트`, `새롭게 알게 된 나`, `요즘 달라진 나`.
- 톤은 친근하고 약간 밈스럽되 자학적이거나 신뢰를 해칠 만큼 가볍지 않아야 한다.
- 브랜딩 논의의 철학적 대안이었던 `나업뎃중`의 장점—사람은 고정되지 않고 계속 변한다—은 제품 카피와 UX에 유지한다.
- 앱 아이콘/로고는 `najalal_logo.png` 및 나잘알 런처·알림 리소스가 현재 프로젝트에 존재한다.

## 3. 기술·프로젝트 개요

- 프로젝트 루트: `C:\Users\sorim\Project\app-block`
- Kotlin + Jetpack Compose Android 앱
- 최소 Android 8.0(API 26), target SDK 35, JDK 17
- 기능별 Room 로컬 DB
- AI 호출: Android 앱 → Firebase Callable Function(`asia-northeast3`) → OpenAI API
- 유튜브 실제 영상 분석 경로에는 Gemini provider도 존재한다.
- 개인용 APK 설치가 우선이며, Android Studio 없이도 JDK 17, Android SDK Command-line Tools, platform-tools(`adb`), Gradle Wrapper로 빌드·설치할 수 있다.
- 기본 확인 명령:

```text
gradlew.bat testDebugUnitTest
gradlew.bat assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk
```

## 4. 유튜브 분석 UX 개편의 확정 방향

### 정보 구조

기존 `홈 / 사고 지도 / 탐색 / 리포트 / 기록` 5개 탭을 없애고 다음 두 축만 유지한다.

1. `뇌 지도`
2. `관심 변화`

`관점 추천`, `주제별 노드 지도`, `공유 URL 수동 분석`은 독립 탭이 아니라 위 두 화면에서 맥락에 맞게 진입한다. 사용자가 유튜브 분석에 들어오면 설명 카드 목록 대신 뇌 지도가 바로 보여야 한다.

### 뇌 지도

- 기간 필터는 MVP에서 `이번 주 / 이번 달 / 올해`.
- 의미 있게 시청한 영상의 주제별 비중과 영상 수를 뇌 형태의 데이터 지도에 표현한다.
- 실제 의학적 좌뇌·우뇌 기능처럼 오해시키지 않는다. 뇌는 관심 분포를 위한 시각적 메타포다.
- 주제 영역은 터치 가능해야 하며, 터치하면 해당 주제의 생각 지도(노드 트리)로 이동한다.
- 최종 구현은 정적 PNG보다 Compose Canvas + Vector/SVG Path + 클릭 가능한 영역이 적합하다.
- 성공 기준: 처음 보는 사용자가 3초 안에 “내가 어떤 주제를 많이 봤는지 보여주는 화면”임을 이해한다.

### 주제별 생각 지도

- 중심은 영상 목록이 아니라 **관점 노드**다.
- 본/탐색한 관점은 초록 또는 보라, 아직 보지 않은 추천 관점은 회색으로 구분한다.
- 노드와 연결선은 실제 `ThoughtNode`/`ThoughtEdge` 관계를 반영한다.
- 노드를 누르면 다음을 보여준다.
  - 그 관점을 접한 원본 영상과 채널
  - 관점의 핵심 설명
  - 다음에 확인할 구체적인 질문
  - 검증된 추천 영상
- 노드 트리는 시청 기록 UI가 아니라 생각 탐색 UI다.

### 관심 변화

- `월별 / 연도별` 전환을 제공한다.
- 세로 리스트와 작은 막대그래프로 각 기간의 상위 주제 및 비율을 보여준다.
- 이후 단계에서 전월 대비 증감과 `NEW` 표시, 짧은 변화 코멘트를 추가한다.
- 장문의 AI 분석 리포트는 MVP에서 제외한다.
- 성공 기준: 사용자가 “5월에는 부동산을 많이 봤지만 요즘은 AI가 늘었다” 같은 변화를 3초 안에 이해한다.

### 제거·비노출 원칙

- 전체 YouTube 시청 기록, 최근 시청 영상 리스트, 별도 기록 탭을 메인 UX에서 제거한다.
- 내부 DB의 영상 이력은 집계·분석·근거 연결을 위해 보존한다.
- 별도 정보 편식 리포트 탭, 탐색 탭, 주제 관리 메인 탭을 제거한다.
- `Gemini`, `Terra`, `MediaSession`, `정밀 분석`, 공개 메타데이터 같은 구현 용어를 일반 화면에서 숨긴다.
- 주제 관리는 필요 시 설정 성격의 보조 진입점이나 영상별 `주제 수정`으로만 제공한다.

## 5. 관점 추천 로직

추천은 두 종류를 구분한다.

### A. 같은 주제의 더 깊은 관점

`형평성`, `정책 효과`, `시장 영향` 같은 포괄적 카테고리명을 금지한다. 실제 다음 검색에 쓸 수 있는 검증 가능한 질문을 만든다.

예:

- 나쁨: `정책 효과`
- 좋음: `이 제도 시행 전후 서울 아파트 거래량은 실제로 어떻게 달라졌을까?`
- 나쁨: `형평성`
- 좋음: `1주택 장기보유자와 다주택자의 실효세율 차이는 얼마나 날까?`

권장 구조:

```text
shortLabel
question
reason
searchQueries[]
```

추천 영상은 검색 결과를 그대로 노출하지 않는다. 후보 검색 → 메타데이터 필터 → 상위 후보 검증 → 해당 관점을 실제로 설명하는 영상만 추천하는 흐름을 지향한다.

### B. 다른 주제 추천

- 최근 전체 관심 분포를 보고 사용자가 거의 접하지 않은 다른 주제를 제안한다.
- 1차 노출 위치는 푸시가 아니라 뇌 지도 아래의 `요즘 안 보던 쪽으로` 카드다.
- 사용 패턴으로 가치가 검증된 뒤에만 푸시 실험을 고려한다.
- 연령대와 성별은 사용자가 명시적으로 동의한 경우에만 보조 신호로 사용한다.
- `개인정보를 활용한 맞춤 추천` ON/OFF가 필요하다.
- “37세 남성이므로 봐야 한다”처럼 인구통계로 사용자를 단정하지 않는다.

## 6. 알림 정책

- 의미 있는 시청의 MVP 기준은 `watchedSec >= 300`(5분)이다.
- 향후 짧은 영상 보완 조건으로 `watchedSec >= 300 OR (watchedRatio >= 0.6 AND duration >= 180)`을 검토할 수 있으나 현재 확정 기준은 단순 5분이다.
- 관점 추천 푸시는 기본 하루 1회, 최대 2회이며 최소 6시간 간격을 둔다.
- 같은 영상/주제 반복 발송을 피한다.
- 최종 목표는 5분 본 영상마다 즉시 보내는 방식이 아니라, 하루 후보를 모아 “확장 가치”가 높은 1~2개만 고르는 방식이다.
- 푸시 문구도 포괄적 안내보다 원본 영상에서 파생된 구체적 질문을 보여준다.
- 다른 주제 추천은 초기에는 푸시하지 않는다.

## 7. Share Intent 수동 분석

자동 감지가 실패하거나 사용자가 직접 분석을 원할 때의 필수 fallback이다.

흐름:

```text
YouTube 공유 → 나잘알 선택 → URL/썸네일 확인 → [분석하기]
→ 구체적인 관점 최대 4개 생성 → 주제별 생각 지도에 반영
```

Android 수신 범위:

- `ACTION_SEND`
- MIME `text/plain`
- `youtube.com/watch?v=...`, `youtu.be/...`, `youtube.com/shorts/...`

공유했다는 사실만으로 자동 분석하지 않고 확인 화면에서 사용자가 `분석하기`를 눌러야 한다. 잘못된 URL, 공개 정보 조회 실패, 중복 영상, 주제 미분류/미승인 흐름도 명시적으로 처리해야 한다.

## 8. 데이터 모델 기준

현재 Room DB `perspective.db`에 존재하는 핵심 테이블:

- `perspective_topics`
- `topic_suggestions`
- `watched_videos`
- `video_topics`
- `video_analyses`
- `perspectives`
- `perspective_recommended_videos`
- `thought_nodes`
- `thought_edges`
- `expansion_moments`
- `weekly_perspective_reports`

기간별 집계의 목표 개념:

```text
TopicExposure {
  topicId
  periodType: week | month | year
  periodKey
  meaningfulVideoCount
  totalWatchSeconds
  percentage
}
```

현재 구현은 이 모델을 DB 테이블로 저장하지 않고 화면에서 `watched_videos + video_topics`를 즉석 집계한다. 데이터량·성능·재현성을 확인한 뒤 집계 테이블 또는 DAO 집계 쿼리 도입을 결정한다.

관점 추천의 목표 개념:

```text
PerspectiveRecommendation {
  id
  sourceVideoId?
  type: same_topic | cross_topic
  shortLabel
  question
  reason
  searchQueries[]
  status: suggested | selected | dismissed
}
```

현재 `PerspectiveEntity`는 `label`, `description`, `representativeQuestion`, 단일 `searchQuery`, `status`를 저장한다. Cross-topic 추천과 여러 검색어, 추천 이유를 정식 지원하려면 기존 모델 확장 또는 별도 엔터티 및 Room migration이 필요하다.

## 9. 2026-08-18 현재 구현 상태

작업 트리에 유튜브 개편 관련 **미커밋 변경이 존재한다. 기존 작업을 덮어쓰거나 되돌리지 말 것.** 현재 확인된 상태는 다음과 같다.

### 이미 구현 또는 반영 중

- 프로젝트 루트 `AGENTS.md`가 Codex에게 이 문서를 먼저 읽도록 연결한다.
- `PerspectiveScreen`을 `뇌 지도 / 관심 변화` 2탭으로 대폭 단순화.
- `이번 주 / 이번 달 / 올해`별 뇌 지도 노출 집계.
- 월별 최대 12개월, 연도별 관심 비중 리스트.
- 뇌 주제 터치 → 주제별 관점 지도 → 관점 상세/추천 영상 진입.
- 의미 있는 시청 기준을 2분에서 5분(`300초`)으로 변경.
- 기존 주제의 관점 제안 알림을 하루 최대 2회, 최소 6시간 간격으로 제한.
- 관점 생성 프롬프트에 구체적·검증 가능한 질문, 짧은 레이블, 포괄 표현 금지 조건 추가(`perspective-analysis-v3`).
- Android `ACTION_SEND text/plain` 수신과 YouTube URL 추출.
- 공유 URL 확인 다이얼로그와 수동 분석 진입.
- 공유 전용 분석은 시청시간을 0초로 저장하고 관심 통계·관점 알림에서 제외한다. 이후 실제 재생이 감지되면 일반 시청 기록으로 승격한다.
- 새 주제 알림 본문을 눌러 앱에 들어온 경우 새 2탭 화면 위에서 주제 등록/건너뛰기 다이얼로그를 제공한다.
- 사용자 화면에서 Terra/Gemini 모델명 노출을 일부 제거.
- Firebase YouTube resolver가 제목, 채널, 썸네일을 반환하도록 확장.

### 아직 초기 구현 또는 미완료

- 뇌 지도는 실제 가변 영역 분할이 아니라 뇌 외곽선 안의 최대 5개 원형 버블 PoC다. 터치 판정도 원 중심 거리 기반이다.
- 생각 지도는 최대 8개 관점을 중심 주위에 방사형 배치하며 실제 `thought_edges` 구조를 그리지 않는다.
- `요즘 안 보던 쪽으로`는 고정 후보 문자열이며 실제 관심 분포, 프로필, AI 추천과 연결되지 않았다.
- 관심 변화는 기간별 비율만 제공하며 전월 대비 증감, `NEW`, 변화 코멘트가 없다.
- 알림은 하루 후보를 모아 가치 점수로 상위 1~2개를 고르는 구조가 아니다. 현재는 자격을 만족한 순서대로 슬롯을 차지한다.
- 동일 영상/동일 주제 중복 알림 방지는 상태 흐름과 함께 추가 검증이 필요하다.
- 주제 등록 제안 알림과 관점 제안 알림의 정책이 분리돼 있어, “하루 1~2회”가 어떤 알림군에 적용되는지 UX 관점에서 통합 점검이 필요하다.
- Share Intent의 주제 자동 승인·연결, 중복 분석, 실패/재시도 흐름을 실제 기기에서 검증해야 한다.
- Cross-topic 추천 데이터 모델, 개인정보 동의 설정, 실제 추천 엔진은 미구현이다.
- 기간 집계는 영상 수 기준이다. 시청 시간 비중을 병행할지 제품 결정을 내려야 한다.
- 기존 `docs/perspective-expansion-spec.md`와 루트 `README.md`의 유튜브 설명은 5탭·2분 기준 등 이전 내용이 남아 있어 개편 완료 후 동기화가 필요하다.

## 10. 개발 우선순위와 TODO

### P0 — 현재 개편을 안전하게 완성

- [ ] 현재 미커밋 변경을 검토하고 컴파일·단위 테스트를 통과시킨다. 앱 컴파일과 `assembleDebug`는 2026-08-18 성공했으며, 전체 단위 테스트는 별도 `MorningDigestScheduleTest`의 `EVENING` 참조 오류로 중단된 상태다.
- [ ] 실제 기기에서 기존 MediaSession 감지, 5분 임계값, 화면 진입을 검증한다.
- [ ] Share Intent의 URL 유형, 취소/확인, 중복 영상, 분석 실패를 검증한다.
- [x] 공유 분석이 시청 통계를 오염시키지 않도록 `source = share`를 통계·관점 알림에서 제외하고 실제 자동 재생 감지 시 `auto`로 승격한다.
- [x] 제거된 주제 관리 화면으로 향하던 알림 진입을 새 2탭 화면의 주제 승인 다이얼로그로 연결한다.
- [ ] 접근성, 작은 화면, 긴 주제명, 빈 데이터, 주제 1개/5개 초과 상태를 확인한다.

### P1 — 핵심 UX 품질

- [ ] 클릭 가능한 실제 뇌 영역 PoC를 만든다. 시각 품질을 먼저 승인받은 뒤 실데이터를 연결한다.
- [ ] 뇌 영역의 비율 표현 규칙(면적, 색 강도, 레이블)을 정하고 합계/기타 처리 규칙을 명시한다.
- [ ] 생각 지도에 실제 `ThoughtEdge` 연결 관계와 탐색 순서를 반영한다.
- [ ] 관점 상세에서 근거 영상 썸네일, 핵심, 여러 다음 질문, 추천 영상 목록을 안정적으로 보여준다.
- [ ] 관심 변화에 전월 대비 증감과 신규 주제 표시를 추가한다.
- [ ] 포괄적 관점이 다시 생성되지 않도록 프롬프트/파서 단위 테스트와 실제 샘플 평가 세트를 만든다.

### P2 — 알림·추천 고도화

- [ ] 일일 후보 저장, 확장 가치 점수, 상위 1~2개 선택 배치를 구현한다.
- [ ] 기본 1회/최대 2회 정책과 동일 영상·주제 재발송 금지를 데이터로 보장한다.
- [ ] 실제 최근 관심 분포 기반 Cross-topic 추천을 구현한다.
- [ ] 연령대·성별 사용 동의와 맞춤 추천 ON/OFF 설정을 추가한다.
- [ ] Cross-topic 추천은 앱 내 카드 사용성을 먼저 측정하고 푸시는 이후 실험한다.

### P3 — 문서·정리

- [ ] `README.md`와 `docs/perspective-expansion-spec.md`를 최종 2탭/5분/Share Intent 기준으로 갱신한다.
- [ ] 더 이상 접근할 수 없는 이전 화면·코드와 사용하지 않는 리포트/주제 관리 UI를 정리하되 DB 데이터는 보존한다.
- [ ] 집계 성능이 필요하면 DAO 집계 쿼리 또는 `TopicExposure` 저장 모델과 migration을 도입한다.
- [ ] APK 빌드 → USB/adb 설치 → 실행 → logcat 확인의 반복 개발 흐름을 검증한다.

## 11. MVP 완료 기준

다음 흐름이 실제 기기에서 자연스럽게 이어지면 이번 유튜브 UX 개편의 MVP가 완료된 것으로 본다.

1. 유튜브 분석에 들어가면 내 관심 분포가 뇌 지도에 바로 보인다.
2. 기간을 주/월/연으로 바꾸면 분포가 일관되게 갱신된다.
3. 주제 영역을 누르면 해당 주제의 실제 관점 연결 지도가 열린다.
4. 아직 보지 않은 구체적인 질문을 누르면 적합한 추천 영상을 확인할 수 있다.
5. 추천 영상을 실제로 5분 이상 본 뒤 탐색 완료 상태와 연결이 갱신된다.
6. 관심 변화에서 월별·연도별 변화가 즉시 이해된다.
7. 자동 감지가 안 되는 영상은 YouTube 공유 메뉴에서 나잘알로 보내 수동 분석할 수 있다.
8. 관점 알림은 과하지 않고, 하루 최대 2회·최소 6시간 간격 및 중복 방지 규칙을 지킨다.
9. 사용자 화면에는 내부 AI 모델명이나 수집 기술 설명이 전면 노출되지 않는다.

## 12. 다음 Codex 작업 시 주의사항

- 이 문서는 현재 개편 방향의 우선 기준이다. 이전 `docs/perspective-expansion-spec.md`와 충돌하면 이 문서와 최신 사용자 요구를 우선하되, 코드를 확인한 뒤 수정한다.
- 작업 트리가 이미 수정된 상태이므로 반드시 `git status`와 관련 diff를 먼저 읽는다.
- 기존 사용자 변경을 임의로 되돌리거나 대규모 덮어쓰기를 하지 않는다.
- 화면만 보고 “구현 완료”로 판단하지 않는다. DB 상태, 알림 deep link, 실제 MediaSession/Share Intent, Firebase 응답까지 종단 흐름을 확인한다.
- 제품 판단이 필요한 쟁점—뇌 영역 표현법, 영상 수와 시청 시간 중 비율 기준, 공유 영상의 통계 포함 여부, 프로필 정보 활용—은 임의 확정하지 말고 명시적인 선택지와 영향을 제시한다.
- 건강검진 상담 등 앱 개발과 무관한 대화 내용은 이 프로젝트 컨텍스트에 포함하지 않는다.
